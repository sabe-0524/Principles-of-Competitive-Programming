#include <iostream>
using namespace std;
using ll = long long;

int main(void)
{
  ll a, b, c;
  cin >> a >> b >> c;
  if (a + b + c == 0)
    cout << "Yes" << endl;
  else
    cout << "No" << endl;
  return (0);
}